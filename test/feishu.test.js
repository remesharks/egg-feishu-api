'use strict';

describe('test/feishu.test.js', () => {

  describe('custom promise', () => {
    it('should load custom promise', function () {
      // const query = app.model.User.findOne({});
      // assert.equal(query.exec().constructor, require('bluebird'));
    });
  });

});
